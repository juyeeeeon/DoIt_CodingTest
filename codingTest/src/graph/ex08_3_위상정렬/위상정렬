위상 정렬(topology sort)
	:사이클이 없는 방향 그래프에서 노드 순서를 찾는 알고리즘
	
	기능 : 노드 간의 순서를 결정
	특징 : 사이클이 없어야 함
	시간복잡도 : O(V+E)				V:노드 수, E:엣지 수
	
	
	* 위상 정렬에서는 항상 유일한 값으로 정렬되지 않음(정답이 여러 개)
	* 진입 차수 배열 이용 (자기 자신을 가리키는 엣지의 갯수)
	
	1) 그래프를 ArrayList로 표현(사이클이 없어야 함)
	2) 진입 차수 배열을 업데이트
	
	(Queue 활용)
	3) 진입 차수 배열에서 진입 차수가 0인 노드를 선택하고, 선택된 노드를 정렬 배열(결과)에 저장
	4) 인접 리스트에서 선택된 노드가 가리키는 노드들의 진입 차수를 1씩 뺌
	5) 진입 차수 배열이 모두 0일 때 까지 3)~4) 반복